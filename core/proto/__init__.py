"""Core proto package — canonical location for generated protobuf Python files.
core proto 包 — 生成的 protobuf Python 文件的规范位置。

The .proto source files live in ``backend/proto/``.
Run ``bash backend/proto/generate.sh`` to regenerate from source.
.proto 源文件位于 ``backend/proto/``。
运行 ``bash backend/proto/generate.sh`` 从源码重新生成。
"""
